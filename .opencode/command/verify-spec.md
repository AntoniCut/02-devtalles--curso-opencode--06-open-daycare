---
description: Verify the acceptance criteria of a spec and mark its checklist.
agent: spec-verifier
---

Verify the acceptance criteria of the spec `$ARGUMENTS` (accepts the full file name like `01-feed-home.md`, the number like `01`, or the slug like `feed-home`).

Follow your verification workflow completely: read the spec and list its criteria, classify them, verify with Playwright/Context7/bash as appropriate, apply minimal code fixes if a criterion fails and re-verify, mark the checkboxes in the spec's Acceptance criteria section, and report in Spanish with evidence for each criterion.
